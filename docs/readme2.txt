Target SDK may need to be set to 29 for this to work.
Android 3.6.1 may also be needed to run this project.

Also warnings that appear when reading the csv file in the logcat are intentional
Rather than ignoring the issues, we chose to show a warning without affecting the app itself